[package]
name = "bismuth_plain_text"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.75"
dbg-pls = { version = "0.4.3", features = ["pretty", "colors", "derive"] }
